import React from 'react'
import Layout from '../components/Layout'

export default function Help() {
  return (
    <Layout
    TitleContent={() => (<></>)}
    FooterContent={() => (<></>)}
  >
    <div>Help</div>
  </Layout>
  )
}
